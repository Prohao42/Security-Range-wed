# CRLF注入 靶场

## 靶场说明

尝试抓包跟踪HTTP请求和响应，利用CRLF注入漏洞构造特定的钓鱼链接使用户访问后会跳转到指定的页面。满足要求后登录系统可以获取秘密字符串。

**说明**：PHP7+版本已修复了CRLF注入漏洞，本靶场是通过代码模拟方式实现了CRLF注入漏洞的效果。由于PHP的Set-Cookie头位置无法指定，这里使用X-User-Name头来模拟从GET请求接收用户名并输出到HTTP响应头的过程（实际场景通常是输出到Set-Cookie中用于设置cookie记住用户名），大家仅关注漏洞利用过程即可。
---

*专注网安实战，掌控安全*